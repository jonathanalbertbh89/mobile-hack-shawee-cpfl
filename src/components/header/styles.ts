import { StyleSheet } from  'react-native'

const styles = StyleSheet.create({
    container:{
        padding: 15,
        alignItems: 'center',
        justifyContent: 'space-between',
        flexDirection: 'row',

        width: '100%',
        height: 85,

        backgroundColor: '#00ade1'
    },

    logo: {
        
       resizeMode: "stretch",
       width: 85,
       height: 60,

        
    }
})

export default styles